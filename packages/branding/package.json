{
  "name": "@packages/branding",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --coverage --passWithNoTests src --coverage.include \"src/**/*.{ts,tsx}\" --coverage.reportsDirectory coverage",
    "test:watch": "vitest src"
  },
  "devDependencies": {
    "@ydm-agency/config": "workspace:*",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
